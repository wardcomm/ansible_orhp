- name: Create a IFS directory if it does not exist
  ansible.builtin.file:
    path: '{{ item }}'
    state: directory
    mode: '0755'
  loop:
    -  /IFS
    -  /IFS/archive
    -  /IFS/transport
    -  /IFS/files
    -  /IFS/scripts